<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hackyle.blog.customer.module.article.mapper.AuthorMapper">

    <resultMap id="BaseResultMap" type="com.hackyle.blog.customer.module.article.model.entity.AuthorEntity">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
        <result property="realName" column="real_name" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="birthday" column="birthday" jdbcType="TIMESTAMP"/>
        <result property="gender" column="gender" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="BIT"/>
        <result property="deleted" column="deleted" jdbcType="BIT"/>
        <result property="createBy" column="create_by" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="BIGINT"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectByArticleIds" resultType="com.hackyle.blog.customer.module.article.model.dto.AuthorDto">
        SELECT  aar.article_id, aar.author_id,
                aa.nick_name, aa.real_name, aa.description
        FROM article_author_relation aar
        JOIN article_author aa ON aar.author_id=aa.id
        <where>
            aar.article_id IN
            <!-- foreach标签：遍历集合 -->
            <!-- collection：指定要遍历的集合 -->
            <!-- item：临时保存当前遍历出的元素，类似于for(int i=0; i<10; i++)中的i -->
            <!-- separator：每个元素之间的分割符 -->
            <!-- open：遍历出的所有结果拼接一个开始字符 -->
            <!-- close：遍历出的所有结果拼接一个结束符 -->
            <foreach collection="articleIds" item="id" separator="," open="(" close=")" >
                <!-- #{i}: 取出元素 -->
                #{id}
            </foreach>
        </where>
    </select>


</mapper>
