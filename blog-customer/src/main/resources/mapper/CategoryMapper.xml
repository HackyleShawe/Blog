<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hackyle.blog.customer.module.article.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.hackyle.blog.customer.module.article.model.entity.CategoryEntity">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="code" column="code" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="color" column="color" jdbcType="VARCHAR"/>
        <result property="iconUrl" column="icon_url" jdbcType="VARCHAR"/>
        <result property="pid" column="pid" jdbcType="BIGINT"/>
        <result property="status" column="status" jdbcType="BIT"/>
        <result property="deleted" column="deleted" jdbcType="BIT"/>
        <result property="createBy" column="create_by" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="BIGINT"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>
    <select id="selectByArticleIds"   resultType="com.hackyle.blog.customer.module.article.model.dto.CategoryDto">
        SELECT acr.category_id, acr.article_id,
               ac.code, ac.name, ac.color, ac.description
        FROM article_category_relation acr
        JOIN article_category ac ON acr.category_id=ac.id
        <where>
            acr.article_id IN
            <!-- foreach标签：遍历集合 -->
            <!-- collection：指定要遍历的集合 -->
            <!-- item：临时保存当前遍历出的元素，类似于for(int i=0; i<10; i++)中的i -->
            <!-- separator：每个元素之间的分割符 -->
            <!-- open：遍历出的所有结果拼接一个开始字符 -->
            <!-- close：遍历出的所有结果拼接一个结束符 -->
            <foreach collection="articleIds" item="id" separator="," open="(" close=")" >
                <!-- #{i}: 取出元素 -->
                #{id}
            </foreach>
        </where>
    </select>


</mapper>
